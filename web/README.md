# Openstackr: a flask client

## Overview

To run the server on a Docker container, please execute the following from the root directory:

```bash
# building the image
docker build -t openstackr .

# starting up a container
docker run -p 5000:5000 openstackr
```
